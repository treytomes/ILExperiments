namespace Mixins.Method2
{
	/// <summary>
	/// use 'M' prefix to indicate mixin interface.
	/// </summary>
	public interface MAgeProvider
	{
		// nothing needed in here, it's just a 'marker' interface.
	}
}